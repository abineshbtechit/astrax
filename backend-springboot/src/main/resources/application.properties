# AstraX Legal & Evidentiary DMS - Spring Boot Application Configuration

# Server Port
server.port=${PORT:8085}

# Spring Application Name
spring.application.name=astrax-legal-dms-backend

# Cloud Database Configuration (Supports Azure Cosmos DB for MongoDB or MongoDB Atlas via MONGODB_URI)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://pfztsxmffwqbsu:J8jdtBg^65$b0In3@docdb-cluster-20260922-1444.global.mongocluster.cosmos.azure.com/?tls=true&authMechanism=SCRAM-SHA-256&retrywrites=false&maxIdleTimeMS=120000}
spring.data.mongodb.database=${MONGODB_DATABASE:ncrb_legal_dms}

# File Upload Limits (for legal evidence and scanned documents)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Actuator Health Endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Logging Configuration
logging.level.com.astrax.legal=INFO
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
